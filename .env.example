# Django
DJANGO_SETTINGS_MODULE=config.settings.local
DJANGO_SECRET_KEY=change-me-only-in-local-development
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL local (compatível com compose.yaml)
DATABASE_URL=postgresql://codigo_futuro:codigo_futuro@localhost:5432/codigo_futuro

# E-mail local
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=nao-responda@codigodofuturo.local
